# Levanta TODO (backend + frontend) con un solo comando:
#   docker compose up --build
# Luego abre en el navegador:  http://localhost:8000
services:
  saludia:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # Para activar el chatbot, descomenta y pon tu clave de OpenAI:
      # - OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
